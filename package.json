{
  "name": "tokenpeek",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Decode, inspect, verify, crack and forge JSON Web Tokens entirely in your browser — nothing is ever uploaded.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-pwa": "^0.21.0",
    "vitest": "^3.2.6",
    "@types/node": "^22.0.0",
    "jsdom": "^25.0.0"
  }
}
